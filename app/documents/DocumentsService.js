'use strict';

angular
    .module('pepeApp')
        .controller(
            'DocumentsController',
            [
                '$scope',
                function ($scope) {
                    $scope.temp = "xxx";
                }
            ]
        );
